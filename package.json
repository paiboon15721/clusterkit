{
  "name": "clusterkit",
  "version": "1.0.0",
  "description": "ClusterKit Webpage",
  "main": "index.js",
  "scripts": {
    "test": "echo \"no test\"",
    "deploy": "rimraf public_html && hugo --minify && mv public public_html && find public_html -type f -exec curl --ftp-create-dirs -T {} ftp://${CLUSTERKIT_FTP_USER}:${CLUSTERKIT_FTP_PASSWORD}@ftp.clusterkit.co.th/domains/clusterkit.co.th/{} \\;",
    "build": "rimraf public && hugo --minify"
  },
  "author": "Paiboon Auengkongkatong",
  "license": "ISC",
  "homepage": "https://paiboon15721.github.io/clusterkit",
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.9.0",
    "bootstrap": "^4.3.1",
    "gh-pages": "^2.0.1",
    "postcss-cli": "^6.1.2",
    "rimraf": "^2.6.3"
  }
}
